<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: #eff1f1;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        .admin-container {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
            padding: 40px;
            width: 100%;
            max-width: 500px;
            transform: translateY(0);
            transition: all 0.3s ease;
        }

        .admin-container:hover {
            transform: translateY(-5px);
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.15);
        }

        .header {
            text-align: center;
            margin-bottom: 30px;
        }

        .header h1 {
            color: #2c3e50;
            font-size: 2.5rem;
            font-weight: 300;
            margin-bottom: 10px;
            background: linear-gradient(45deg, #667eea, #764ba2);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .welcome-text {
            color: #7f8c8d;
            font-size: 1.1rem;
            font-weight: 400;
        }

        .user-email {
            color: #667eea;
            font-weight: 600;
        }

        .stats {
            background: linear-gradient(45deg, #f8f9fa, #e9ecef);
            border-radius: 15px;
            padding: 20px;
            margin-bottom: 30px;
            text-align: center;
        }

        .stats p {
            margin: 8px 0;
            font-size: 1.1rem;
            color: #2c3e50;
        }

        .nav-menu {
            list-style: none;
            margin-top: 30px;
        }

        .nav-item {
            margin-bottom: 15px;
            opacity: 0;
            animation: slideInUp 0.6s ease forwards;
        }

        .nav-item:nth-child(1) { animation-delay: 0.1s; }
        .nav-item:nth-child(2) { animation-delay: 0.2s; }
        .nav-item:nth-child(3) { animation-delay: 0.3s; }
        .nav-item:nth-child(4) { animation-delay: 0.4s; }
        .nav-item:nth-child(5) { animation-delay: 0.5s; }

        .nav-link {
            display: block;
            padding: 15px 20px;
            text-decoration: none;
            color: #2c3e50;
            background: linear-gradient(45deg, #f8f9fa, #e9ecef);
            border-radius: 12px;
            font-weight: 500;
            font-size: 1.1rem;
            transition: all 0.3s ease;
            border: 2px solid transparent;
            position: relative;
            overflow: hidden;
        }

        .nav-link::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(45deg, #667eea, #764ba2);
            transition: left 0.3s ease;
            z-index: -1;
        }

        .nav-link:hover {
            color: white;
            transform: translateX(10px);
            box-shadow: 0 10px 20px rgba(102, 126, 234, 0.3);
        }

        .nav-link:hover::before {
            left: 0;
        }

        .nav-link.logout {
            background: linear-gradient(45deg, #ff6b6b, #ee5a52);
            color: white;
            margin-top: 20px;
        }

        .nav-link.logout::before {
            background: linear-gradient(45deg, #ff5252, #d32f2f);
        }

        .nav-link.logout:hover {
            transform: translateX(10px);
            box-shadow: 0 10px 20px rgba(255, 107, 107, 0.4);
        }

        @keyframes slideInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .decorative-dots {
            position: absolute;
            top: 20px;
            right: 20px;
            width: 60px;
            height: 60px;
            opacity: 0.1;
            background: radial-gradient(circle, #667eea 2px, transparent 2px);
            background-size: 10px 10px;
        }

        @media (max-width: 480px) {
            .admin-container {
                padding: 30px 20px;
                margin: 10px;
            }
            
            .header h1 {
                font-size: 2rem;
            }
            
            .nav-link {
                font-size: 1rem;
                padding: 12px 16px;
            }
        }
    </style>
</head>
<body>
    <div class="admin-container">
        <div class="decorative-dots"></div>
        
        <div class="header">
            <h1>Admin Dashboard</h1>
            <div class="stats">
                <p><strong>👥 Total Users:</strong> <%= totalUsers %></p>
                <p><strong>🛍️ Total Orders:</strong> <%= totalOrders %></p>
                <p><strong>💰 Total Revenue:</strong> $<%= totalRevenue.toFixed(2) %></p>
            </div>
        </div>

        <ul class="nav-menu">
            
            <li class="nav-item">
                <a href="/admin/products" class="nav-link">📦 Manage Products</a>
            </li>
            <li class="nav-item">
                <a href="/admin/products/add" class="nav-link">➕ Add New Product</a>
            </li>
            <li class="nav-item">
                <a href="/admin/vehicles" class="nav-link">🚗 Manage Vehicles</a>
            </li>
            <li class="nav-item">
                <a href="/admin/orders" class="nav-link">🛍️ View Orders</a>
            </li>
            <li class="nav-item">
                <a href="/logout" class="nav-link logout">🚪 Logout</a>
            </li>
        </ul>
    </div>
</body>
</html>